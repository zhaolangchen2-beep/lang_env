# 子模块依赖描述
module_info:
  name: "cpython_test_module"
  version: "1.0.0"

# 1. 软件下载依赖：deploy.sh 会检查 tmp/ 是否存在这些文件，不存在则去 url.yaml 找对应链接下载
# 这里的 key 必须与 url.yaml 中的 key 对应
software:
  - "cinderx.tgz"


# 4. 模块间依赖（预留功能）
depends_on: []